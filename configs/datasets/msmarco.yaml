dataset_name: msmarco

source: huggingface

hf_dataset: ms_marco

version: "v1.1"

split: dev-small

task_type: passage_retrieval

limits:

  max_queries: null
  max_documents: null

evaluation:

  primary_metric: mrr@10

  secondary_metrics:
    - recall@10
    - map
    - latency_ms

storage:

  raw_dir: data/raw/msmarco
  processed_dir: data/processed/msmarco
  cache_dir: data/cache/msmarco

seed: 42

